import styled from 'styled-components'

import Card from '../../card/card.style'

export const QuickAccessCard = styled(Card)`
  background: ${(p) => p.theme.vars.colors.secondary3_v2};
  padding: 20px 20px;
  margin-bottom: 20px;
`
