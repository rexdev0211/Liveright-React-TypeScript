import styled from 'styled-components'

export default styled.div`
  position: fixed;
  display: flex;
  justify-content: flex-end;
  bottom: 0;
  left: 0;
  right: 0;
  pointer-events: none;
  touch-action: none;
  overflow: hidden;
`
