import styled from 'styled-components'

export const Styles = styled.div`
  padding: 2rem 0;

  .calendar {
    &__title {
      &-container {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 3rem;
      }
    }
  }
`
